<h2>Perfil de <%= @user.name + ' ' + @user.lastname %> </h2>
<% if session[:user_id] == @user.id %>
	<div>
		<i class="icon-pencil"></i> <%= link_to "Editar datos personales", edit_user_path(@user) %> | 
		<i class="icon-edit"></i> <%= link_to "Editar mi perfil", edit_profile_path %> | 
		<i class="icon-briefcase"></i> <%= link_to "Cambiar contraseña", change_password_path %>
	</div>
<% end %>

<hr/>
<%= raw @user.profile %>
<hr/>