<h2>Viendo los archivos del usuario <%= @user.name%> <%= @user.lastname %> para la tarea "<%= Homework.find(params[:homework_id]).name %>"</h2>
<!-- Mostrar todos los archivos -->
<% if @archives.count > 0%>
<div class="accordion" id="accordion2">
	<% current = 0 %>
	<% @archives.each do |a| %>
		<% if a.version > current %>
			<% if current != 0 %>
			</div>
			<% end %>
		<div class="accordion-group">
			<div class="accordion-heading">
		      <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion<%= a.version%>" href="#collapse<%= a.version%>">
		        Versión <%= a.version%>
		      </a>
		    </div>
		    <div id="collapse<%= a.version%>" class="accordion-body collapse">
		    	<div class="accordion-inner">
			        <%= a.name %>
			      </div>
			<% current = a.version%>
		<% else %>
			<div class="accordion-inner">
		       <%= a.name %>
		      </div>
		<% end %>
	<% end %>
</div>
<% else %>
Este usuario no ha subido archivos a esta tarea.
<% end %>